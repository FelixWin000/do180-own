# Ref: HAPROXY Error

https://github.com/docker-library/haproxy/issues/162

# sudo podman pod create -n webserver -p 5555:8080 -p 5556:8081 -p 5557:8082

# sudo podman run -d --pod webserver --name web1 localhost/web
# sudo podman run -d --pod webserver --name web2 -e PORT=8082 localhost/web
# sudo podman run -d --pod webserver --name web3 -e PORT=8083 localhost/web

# sudo podman pull docker.io/library/haproxy:2.4.3

# mkdir haproxy

# sudo semanage fcontext -a -t container_file_t '/home/student/haproxy(/.*)?'

# sudo restorecon -Rv /home/student/haproxy

# sudo chown -R 99:99 haproxy

# sudo podman run -d --name myhaproxy -p 8080:80 -v /home/student/haproxy/:/usr/local/etc/haproxy:ro --user root  docker.io/library/haproxy:2.4.3

